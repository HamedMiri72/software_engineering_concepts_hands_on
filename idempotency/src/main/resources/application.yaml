server:
  port: 8080          # each concept gets its own port so several can run at once

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/idempotency
    username: se
    password: se
  jpa:
    hibernate:
      ddl-auto: update      # dev only
    show-sql: true
  data:
    redis:
      host: localhost
      port: 6379